# Nike FY2025 analog — Tableau Public

I cannot publish to Haley’s Tableau Public account from here. The extract is ready; publishing is a two-minute click on her machine.

## File

`../data/nike-fy2025-analog.csv`

Public 10-K / earnings figures (FY2025). Analog only — not an employer.

## Publish

1. Create a free [Tableau Public](https://public.tableau.com/) profile.
2. Open Tableau Public (desktop or web authoring) → connect to the CSV.
3. Sheet: `yoy_cn_pct` by `line`, filter `view` = Channel (and a second sheet for Franchise).
4. Dashboard: KPI strip (Revenue −10%, Inventory flat, Direct −13%, Jordan −16%) + the two bars + a note: “Clear Jordan and digital. Hold wholesale.”
5. Save to Tableau Public. Copy the viz URL.

Then add that URL on `nike.html` and the résumé.

Until the public viz exists, the CSV in this repo and the HTML case study are the proof of the analysis.
